# Agent Documentation

All agent-related documentation is now organized in the `.opencode/agents/` directory and linked in `opencode.json`.

Please refer to the `instructions` array in `opencode.json` for the complete list of documentation files.
